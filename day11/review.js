// template literals 

// - là một chuỗi được đặt trong dấu backtick `` thay vì dấu '' hay ""
// - ưu điểm khi sử dụng là : 
    // + có thể nội suy biểu thức bằng cụm ${} 
    // -> các biểu thức js có thể nằm trong cụm này khi trả về dữ liệu sẽ được cộng vào chuỗi

    // + có thể xuống dòng tùy thích miễn chuỗi nằm trong dấu ``
// - sử dụng trong trường hợp : 
    // + các trường hợp chuỗi cần trả về 2 kết quả dựa  trên 1 biểu thức
    // -> giúp code ngắn gọn hơn 

    // + tạo ra khối các phần tử html vì có thể xuống dòng và nội suy
    // -> việc tạo khối thẻ rất dễ dàng với ``

    // + thay đổi class của phần tử dựa trên điều kiện 

    // + thay đổi style css của phần tử dựa trên điều kiệnn 